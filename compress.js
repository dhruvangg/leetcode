var compress = function (chars) {
    let temp;
    let count;
    for (let i = 0; i < chars.length; i++) {
        if(temp && temp !== chars[i]){
            
        }

        count++;
    }
};

console.log(compress(["a", "a", "b", "b", "c", "c", "c"]));
// console.log(compress(["a"]));
// console.log(compress(["a", "b", "b", "b", "b", "b", "b", "b", "b", "b", "b", "b", "b"]));


a 

a == a

